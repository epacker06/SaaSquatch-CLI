# SaaSquatch-CLI

A simple, lightweight Java CLI application built using the `picocli` framework, created for the Fall 2026 Co-op application.

## How to Run
1. Clone the repository.
2. Compile and execute `ImpactCLI.java` with the picocli library in your classpath.
